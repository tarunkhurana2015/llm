# llm

## What is LLM?

A language model (llm) is a function that assigns probabilities to sequences of words.

```python
P(w1, w2,...,wn) => P(w1) * P(w2|w1) * P(w3| w1, w2)....
```

![alt text](image.png)
