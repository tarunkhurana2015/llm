## Environment setup:

    	conda create -n env_minigpt python=3.13
    	conda activate env_minigpt
    	python -m pip install --upgrade pip
    	Install packages:
    	pip install -r requirements.txt
